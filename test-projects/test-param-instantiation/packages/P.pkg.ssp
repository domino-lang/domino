package P {
    params {
        b: Bool,
        y: Integer,
        f: fn Bool -> Bool
    }

    state {
        tea: Maybe(Bits(y))
    }

    oracle Hello(k: Bits(y)) -> Bits(y) {
        tea <- Some(k);
        /* assert f(true); */
        return Unwrap(tea);
    }
}