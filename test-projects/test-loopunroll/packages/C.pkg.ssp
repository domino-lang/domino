package C {
  oracle Test() -> (Integer, Integer, Integer) {
    T <- new Table(Integer, Integer);
    for i: 0 <= i < 3 {
      x <-$ Integer;
      T[i] <- Some(x);
    }
    res <- (Unwrap(T[0]), Unwrap(T[1]), Unwrap(T[2]));
    return res;
  }
}