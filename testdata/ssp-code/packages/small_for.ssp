package SmallForPkg {
    params {
        n: Integer,
    }

    import oracles {
        N() -> Integer,
    }

    oracle Sum() -> Integer {
        sum <- 0;

        for i: 1 <= i <= n {
          n_i <- invoke N();
          sum <- (sum + n_i);
        }

        return sum;
    }
}
