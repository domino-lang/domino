package Client {
    types {
        Elem,
    }

    import oracles {
        GetPair() -> (Elem, Elem),
    }


    oracle Query() -> Elem {
        (x, y) <- invoke GetPair();
        return x;
    }
}
