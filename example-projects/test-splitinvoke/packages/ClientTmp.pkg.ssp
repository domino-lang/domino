package ClientTmp {
    types {
        Elem,
    }

    import oracles {
        GetPair() -> (Elem, Elem),
    }


    oracle Query() -> Elem {
        tmp <- invoke GetPair();
        (x, y) <- tmp;
        return (x and y);
    }
}
