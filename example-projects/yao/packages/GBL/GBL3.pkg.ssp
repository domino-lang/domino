package GBL3 {
    params {
        p: Integer
    }

    import oracles {
        GBL(Ci: Table(Integer, (Integer, Integer, Table((Bool,Bool),Bool)))) 
             -> Table(Integer, Table(Bits(p), Bool)),
    }

    oracle GBL3(Ci: Table(Integer, (Integer, Integer, Table((Bool,Bool),Bool)))) 
             -> Table(Integer, Table(Bits(p), Bool)) {
        g <- invoke GBL(Ci);
        return g;
    }
}